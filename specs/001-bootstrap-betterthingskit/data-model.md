# Data Model: 初始化 BetterThingsKit 共享核心包并接入应用外壳

**Date**: 2026-09-21

本特性**不引入业务数据实体**（见 spec.md §Key Entities）。

- 任务实体（TaskItem）属于特性 BT-8「定义 TaskItem 数据模型」，届时在本目录补
  `data-model.md` 的实体定义。
- 本特性唯一的"状态"是构建产物，无持久化、无状态迁移。
